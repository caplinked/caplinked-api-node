/* eslint-env mocha */

var chai = require('chai');
var sinon = require('sinon');

var assert = chai.assert;

describe('Caplinked', function () {
  var cl;
  describe('test', function () {
    it('runs test', function () {
      assert.equal('werd', 'werd');
    });
  });
});
